# Validate structured Design System Previews

Every Published Design System supplies structured specimen data for the complete Design System Preview through validated publication metadata. The web application uses one shared renderer and discovers each page from published releases without identity specific route branches. This adds publication schema work but avoids fragile extraction from freeform Design Contracts and ensures future Design Systems cannot publish with incomplete public specimens.
