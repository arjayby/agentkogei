interface:
  display_name: "Publish Design System"
  short_description: "Evaluate a candidate and prepare publication"
  default_prompt: "Use $publish-design-system to evaluate this approved candidate and prepare an immutable publication proposal."
policy:
  allow_implicit_invocation: false
